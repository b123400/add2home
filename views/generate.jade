html
  head
    title .
    link(rel="apple-touch-icon" href="#{imageurl}")

    meta(name="viewport" content="width=320px")

  body
    script.
      if (document.referrer == ""){
       location.href ="#{url}";
      }
    p Last Step.
    p Add this page to homescreen.